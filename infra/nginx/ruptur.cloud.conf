# Ruptur Cloud - Nginx
#
# Estratégia SaaS:
# - ruptur.cloud / www.ruptur.cloud: landing page, vendas e institucional
# - app.ruptur.cloud: frontend autenticado/painel
# - api.ruptur.cloud: backend, APIs e webhooks
# - saas.ruptur.cloud: gateway SaaS/compatibilidade

server {
    listen 80;
    server_name ruptur.cloud www.ruptur.cloud;
    # Redirecionar HTTP → HTTPS
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl http2;
    server_name ruptur.cloud www.ruptur.cloud;

    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;

    location = /login {
        return 301 https://app.ruptur.cloud/login;
    }

    location = /signup {
        return 301 https://app.ruptur.cloud/signup;
    }

    location /api/ {
        proxy_pass http://localhost:3001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /warmup/ {
        proxy_pass http://localhost:4173;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /assets/ {
        alias /opt/ruptur/saas/dist-client/assets/;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    location /client-area/ {
        alias /opt/ruptur/saas/dist-client/;
        try_files $uri $uri/ /client-area/index.html;
    }

    location / {
        root /opt/ruptur/saas/dist-client;
        try_files $uri $uri/ /index.html;
    }
}

server {
    listen 80;
    server_name api.ruptur.cloud;
    # Redirecionar HTTP → HTTPS
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl http2;
    server_name api.ruptur.cloud;

    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;

    location /api/ {
        proxy_pass http://localhost:3001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location = /health {
        proxy_pass http://localhost:3001/api/health;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location / {
        return 404;
    }
}

server {
    listen 80;
    server_name app.ruptur.cloud saas.ruptur.cloud;

    # Redirecionar HTTP → HTTPS para esses domínios
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl http2;
    server_name app.ruptur.cloud saas.ruptur.cloud;

    # SSL configuration (deixar em branco se usar TLS termination em load balancer)
    # ssl_certificate /etc/ssl/certs/ruptur.cloud.crt;
    # ssl_certificate_key /etc/ssl/private/ruptur.cloud.key;

    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;

    location /api/ {
        proxy_pass http://localhost:3001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /warmup/ {
        proxy_pass http://localhost:4173;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Rotas SPA da aplicação logada/public auth no subdomínio app.
    location ~ ^/(login|signup|onboarding|dashboard|campanhas|carteira|inbox|admin|403)(/.*)?$ {
        root /opt/ruptur/saas/dist-client;
        try_files /index.html =404;
    }

    location /client-area/ {
        alias /opt/ruptur/saas/dist-client/;
        try_files $uri $uri/ /client-area/index.html;
    }

    location /dashboard/ {
        alias /opt/ruptur/saas/web/dashboard-dist/;
        try_files $uri $uri/ /dashboard/index.html;
    }

    location /manager/ {
        alias /opt/ruptur/saas/web/manager-dist/;
        try_files $uri $uri/ /manager/index.html;
    }

    location /assets/ {
        alias /opt/ruptur/saas/dist-client/assets/;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    location / {
        root /opt/ruptur/saas/dist-client;
        try_files $uri $uri/ /index.html;
    }
}
