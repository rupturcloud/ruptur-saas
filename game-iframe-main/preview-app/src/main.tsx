import React from "react";
import ReactDOM from "react-dom/client";
import "./index.css";
import BacBoMiniplayerPreview from "./BacBoMiniplayerPreview";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <BacBoMiniplayerPreview />
  </React.StrictMode>
);
